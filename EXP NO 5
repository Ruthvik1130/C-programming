/* NAME: RUTHVIK KUNDAR 
BRANCH/YEAR: Computer Engineering/F.E
UIN: 251P130
ROLL NO: 52
DIV: D */
#include <stdio.>
// Iterative method
int factorialIterative(int n) {
    int fact = 1;
    // Factorial of 0 is 1, handled by initial fact = 1
    for (int i = 1; i <= n; i++) {
        fact = fact * i;
    }
    return fact;
}

// Recursive method
int factorialRecursive(int n) {
    // Base case: Factorial of 0 or 1 is 1
    if (n == 0 || n == 1) {
        return 1;
    } else {
        // Recursive step: n! = n * (n-1)!
        return n * factorialRecursive(n - 1);
    }
}

int main() {
    int num;

    printf("Enter a number: ");
    scanf("%d", &num);

    printf("Factorial (Iterative) = %d\n", factorialIterative(num));
    printf("Factorial (Recursive) = %d\n", factorialRecursive(num));

    return 0;
}


